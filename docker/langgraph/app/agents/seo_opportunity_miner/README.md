# SEO Opportunity Miner

Identifies quick-win SEO actions from Search Console data. Expects `search_console_rows` with query-level metrics and a list of `site_pages` for validation. Returns a single prioritized fix focused on titles, headings, and internal links.
